 git的操作流程如下：
1.建立库与本地文档的关系。
如何将本地改动过的文档同步到库中：
  对本地文档使用GUI工具，将改动过的文档从工作区转入到暂存区中，
之后用commit将暂存区的文件同步到本地仓库
之后运用push将本地仓库的文档同步到远程仓库中就完成了本地文件中的改动同步到库中
如何将库中的改动文件同步到本地文件中：
  对库中的改动文件使用GUI工具，然后remote，fetch from(找仓库有什么变化，放到本地仓库），origin。
点击merge（本地仓库的东西出现在工作区）点击第一个local然后merge完成。